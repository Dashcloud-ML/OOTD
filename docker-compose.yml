# docker-compose.yml — project root
# Run the backend in a container with one command:
#   docker compose up --build
# Reads your existing backend/.env directly — no separate config needed.

services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    restart: unless-stopped